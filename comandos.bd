----------------------------  COMANDOS CRIAÇÃO DO BANCO DE DADOS   ----------------------------

CREATE DATABASE projeto_pi;

CREATE TABLE adm_e_usuario (

    id int auto_increment primary key,
    nome_completo varchar(250) not null,
    cpf varchar(14) UNIQUE not null,
    rua varchar(250) not null,
    bairro varchar(250) not null, 
    cidade varchar(250) not null,
    numero varchar(6) not null,
    num_telefone varchar(15) not null,
    email varchar(100) not null,
    senha varchar(250) not null,
    perfil int not null 
);


CREATE TABLE vacina(
    id int auto_increment primary key,
    nome_vacina varchar(250),
    fabricante varchar(250),
    doenca_alvo varchar(300)
);

CREATE TABLE posto_vacinacao(

    id int primary key auto_increment,
    razao_social varchar(250) not null,
    cnpj varchar(20) UNIQUE,
    num_telefone varchar(15) not null,
    email varchar(100) not null,
    rua varchar(200) not null,
    cidade varchar(250) not null,
    bairro varchar(200) not null,
    numero int(100) not null
);

CREATE TABLE vacina_usuario(

    id user
    id vacina
    id posto
);


code whisperer



CRIAR TABELA VACINAS 

create table vacinas(id int auto_increment, nomevacina VARCHAR(250), local VARCHAR(250), fabricante VARCHAR(250),
 funcao_vacina VARCHAR(250), PRIMARY KEY (id));


 EXEMPLO DE VACINA FICTICIA 


 insert into vacinas (nomevacina, local, fabricante, funcao_vacina) values ("Vacina influenza trivalente", "sao leopoldo", "fabricante 1", "Covid");

 insert into vacinas (nomevacina, local, fabricante, funcao_vacina) values ("Vacina ficticia 2", "novo hamburgo", "fabricante 2", "Dengue");


 insert into vacinas (nomevacina, local, fabricante, funcao_vacina) values ("Vacina ficticia 3", "Porto Alegre", "fabricante 4", "Xicungunha");

